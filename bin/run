#!/bin/sh
CURRENT_PATH=$PWD
FILE_PATH=$(dirname $0)
cd $FILE_PATH
g++ -std=c++11 ../src/**/*.cpp ../src/Interpreter.cpp  -o interpreter
./interpreter ../examples/full.tl
rm interpreter
cd $CURRENT_PATH
